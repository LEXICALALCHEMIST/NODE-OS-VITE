// src/warp/manifest.js — Warp Mode Prototype (Demo Ready)

export const WARP_MANIFEST = {
   id: "ARCH-001",
   name: "Archon",
   icon: "▣",
   hub: "http://localhost:3000/apps/ARCHON/archonMain.js",  // Direct stream from ALL-MIND
  componentName: "ArchonSkin"  // Exact exported/global name in remote script
};

// Comment out for desktop/reactor mode testing
 //export const WARP_MANIFEST = null;